{
  "name": "corona-case tracking-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": " concurrently --kill-others --raw  \"cd backend && node index.js\" ",
    "start": "concurrently --kill-others --raw  \"cd backend && node index.js\" ",
    "deploy":"concurrently --kill-others --raw  \"cd backend && node index.js\" ",
    "prod":"concurrently --kill-others --raw  \"cd backend && node index.js\" "
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cheerio": "^1.0.0-rc.10",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-cache-headers": "^0.1.4",
    "express-cache-middleware": "^1.0.1",
    "fs": "^0.0.1-security",
    "http": "^0.0.1-security",
    "redis": "^4.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "eslint": "^7.32.0",
    "nodemon": "^2.0.15"
  },
  "node_engine": {
    "node": ">=10.16.0"
  },
  "browserslist": {
    "production": [
      ">0.3%",
      "not ie 11",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      ">0.3%",
      "not ie 11",
      "not dead",
      "not op_mini all"
    ]
  }
}
